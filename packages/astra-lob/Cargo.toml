[package]
name = "astra-lob"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Deterministic institutional-style limit order book"

[dependencies]
astra-core = { path = "../astra-core" }
serde = { version = "1.0", features = ["derive"] }
blake3 = "1.5"

[lints.rust]
unsafe_code = "forbid"

[lints.clippy]
all = "warn"
